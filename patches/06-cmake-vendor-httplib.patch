diff --git a/vendor/cpp-httplib/CMakeLists.txt b/vendor/cpp-httplib/CMakeLists.txt
--- a/vendor/cpp-httplib/CMakeLists.txt
+++ b/vendor/cpp-httplib/CMakeLists.txt
@@ -15,12 +15,18 @@
 target_include_directories(${TARGET} PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
 target_link_libraries(${TARGET} PUBLIC ${WS2_32_LIBRARY})

target_compile_features(${TARGET} PRIVATE cxx_std_17)

+# Windows 7 compatibility
+if (WIN32 AND LLAMA_WIN7_COMPAT)
+    target_compile_definitions(${TARGET} PRIVATE _WIN32_WINNT=0x0601 WINVER=0x0601)
+    message(STATUS "cpp-httplib: Building with Windows 7 compatibility")
+endif()
+
 set_target_properties(${TARGET} PROPERTIES
     PUBLIC_HEADER "httplib.h"
     OUTPUT_NAME httplib
     POSITION_INDEPENDENT_CODE ON
 )
